extends student

block sub-container
    h1.page-header 软件过程改进
    .table-responsive
        table.table.table-striped
            thead
                tr
                    th 出题老师
                    th 作业标题
                    th 截止时间
                    th 我的分数
                    th 操作
            tbody
                each homework in homeworks
                    tr
                        td #{homework.teacherName}
                        td #{homework.title}
                        td.before-deadline #{homework.deadline}
                        - found = false
                        each one in homework.students
                            if one.name == user.username
                                td #{one.score}
                                - found = true
                        if !found
                            td
                        td
                            a.btn.btn-primary.btn-sm.upload-hw(href='/p/#{homework._id}/#{homework.title}') 查看详情